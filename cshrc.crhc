if (! $?f_gatherpaths) then
    unalias	make
    alias	ups	ps -u ${USER}
    alias	lynx	lynx -cfg=${HOME}/software/lynx2-8-2/lynx.cfg
    alias	gmake	make

    umask 026
    set watch=(1 any any)

    setenv	PRINTER	pssouth

    cstat "done\n"
endif
