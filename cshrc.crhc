unalias make
alias   ps      ps -u ${USER}
alias	lynx	lynx -cfg=${HOME}/software/lynx2-8-2/lynx.cfg
alias   gmake   make

umask 026
set watch=(1 any any)
setenv CVSROOT	/home/titan/CVSROOT
if ($OSTYPE == "hpux") then
   setenv PATH "${PATH}:/home/titan/rational/releases/DeveloperTools.5.2/hppa_hpux/bin"
endif

setenv	PRINTER	pssouth
