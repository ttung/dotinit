# System-wide .cshrc file for the HP/UX systems.
# Set up search path, man pages path, environment, and user name.

#set sys = `uname -n | sed 's/\..*$//'`

if (-d /usr/local/ews) then
	setenv PATH	"/usr/local/ews:$PATH"
endif

if (-d /homesta/classdata/bin) then
	setenv PATH	"/homesta/classdata/bin:$PATH"
endif
